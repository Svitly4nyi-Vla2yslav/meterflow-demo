import { Leaf, Sun, Zap } from 'lucide-react';
import type { PropsWithChildren } from 'react';

export function AuthShell({ eyebrow, title, subtitle, children }: PropsWithChildren<{ eyebrow: string; title: string; subtitle: string }>) {
  return <main className="auth-page"><section className="auth-form-side"><div className="auth-form-wrap"><div className="auth-brand"><span><Leaf /></span><strong>MeterFlow</strong></div><div className="auth-heading"><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{subtitle}</p></div>{children}<p className="auth-footnote">Unabhängiges Portfolio- und Demonstrationsprojekt</p></div></section><section className="auth-visual" aria-label="Nachhaltige Energie für Wohngebäude"><div className="auth-visual__glow" /><div className="auth-visual__copy"><span className="auth-kicker"><Sun /> Energie intelligent nutzen</span><h2>Nachhaltige Energie für Mehrfamilienhäuser</h2><p>Projekte strukturiert planen, Fortschritt transparent machen und die Energiewende gemeinsam voranbringen.</p></div><div className="building-scene" aria-hidden="true"><div className="scene-sun" /><div className="building building--back"><i /><i /><i /><i /></div><div className="building building--front"><div className="solar-roof"><span /><span /><span /></div>{Array.from({ length: 9 }).map((_, index) => <i key={index} />)}<b><Zap /></b></div><div className="scene-ground" /></div></section></main>;
}
